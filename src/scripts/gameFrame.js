class GameFrame {
  constructor(element) {
    this.element = element;
    this.addGameImage();
  }

  addGameImage() {
    const gameDisplay = document.getElementById("game-frame");
    
  }
}

export default GameFrame;